<Window x:Class="Cookmaster.Views.ForgotPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cookmaster.Views"
        mc:Ignorable="d"
        Title="ForgotPasswordWindow" Height="450" Width="800">
    <StackPanel Margin="20">
        <TextBlock Text="Reset Password" 
                   FontSize="20" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <TextBlock Text="Username:"/>
        <TextBox Text="{Binding Username}" Margin="0,5,0,10"/>

        <Button Content="Show security question"
                Command="{Binding LoadSecurityQuestionCommand}"
                Height="30" Width="180"
                HorizontalAlignment="Center"
                Margin="0,0,0,10"/>

        <TextBlock Text="{Binding SecurityQuestion}" 
                   FontStyle="Italic" 
                   Margin="0,10,0,5"
                   TextWrapping="Wrap"/>

        <TextBlock Text="Answer:"/>
        <TextBox Text="{Binding SecurityAnswer}" Margin="0,5,0,10"/>

        <TextBlock Text="New password:"/>
        <PasswordBox x:Name="NewPasswordBox" Margin="0,5,0,10"/>

        <Button Content="Reset password"
                Command="{Binding ResetPasswordCommand}"
                Height="35" Width="150"
                HorizontalAlignment="Center"/>
    </StackPanel>
</Window>
