<Window x:Class="Cookmaster.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Cookmaster.Views"
        mc:Ignorable="d"
        Title="RegisterWindow" Height="450" Width="800">
    <StackPanel>
        <TextBlock Text="Register new user" FontSize="20"/>
        <TextBox Text="{Binding Username}"/>
        <PasswordBox Name="PasswordBox"/>
        <PasswordBox x:Name="ConfirmBox"/>
        <ComboBox ItemsSource="{Binding Countries}" SelectedItem="{Binding SelectedCountry}"/>
        <Button Content="Register" Command="{Binding RegisterCommand}" CommandParameter="{Binding ElementName=PasswordBox, Path=Password}"
                Tag="{Binding ElementName=ConfirmBox, Path=Password}"/>

    </StackPanel>
</Window>
